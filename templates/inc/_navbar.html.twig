<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="#">SORTIR.COM</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="">Home</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="">Sorties</a>
            </li>
            {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                <li class="nav-item">
                    <a class="nav-link" href="">Créer une sortie</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href={{ path("participant_logout") }}>Se déconnecter {{ app.user.username }}</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href={{ path("participant_login") }}>Se connecter</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href={{ path("participant_register") }}>S'inscrire</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>